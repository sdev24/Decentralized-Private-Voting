{
  "name": "voting-circuits",
  "version": "1.0.0",
  "description": "ZK circuits for private voting",
  "scripts": {
    "compile": "circom vote.circom --r1cs --wasm --sym",
    "setup": "bash setup.sh"
  },
  "devDependencies": {
    "circom": "^2.1.6",
    "snarkjs": "^0.7.0",
    "circomlib": "^2.0.5"
  }
}